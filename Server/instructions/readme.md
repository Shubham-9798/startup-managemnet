fix the username, as it shouldn't updatable or changeble
server session
when we do get request is call url from client app show up in url
set pin for login

// register
page => registrationPage -> security ->finish
// store some of master tbl as localstorage

puyblic roues
